import { createAsyncActionTypes } from '../../sagas'

export const GET_TRANSACTION_HISTORY = createAsyncActionTypes('GET_TRANSACTION_HISTORY')
export const TRANSFER = createAsyncActionTypes('TRANSFER')
